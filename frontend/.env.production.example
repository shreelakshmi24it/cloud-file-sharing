# Frontend Environment Variables (Production)
# Copy this file to .env.production

# Backend API URL
# This should point to your production backend
VITE_API_URL=https://api.yourdomain.com

# Application Name
VITE_APP_NAME=SecureCloud

# Build-time environment
# This is automatically set during build, but you can override if needed
# VITE_BUILD_ENV=production
